// Replace All Occurrences of a Word in a String: Given a string and a word, replace
// all occurrences of that word with another word.

function strToChange(res) {
  let mess = "My name is arpit";
  let ans = mess.replace("arpit", res);
  console.log(ans);
}
strToChange("Rohit");
