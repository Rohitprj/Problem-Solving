// Union example

let folowers: string | number = "1m";

let apiStatus: "pending" | "complete" | "error" = "pending";

apiStatus = "complete"; // only accepts these 3

// Avoide any as much as possible
